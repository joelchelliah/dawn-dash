export interface SpecialWeapon {
  id: number
  name: string
  category: string
  type: string
  isTreasure: boolean
  fromTreasureEvents: string[]
  fromEvents: string[]
  fromCards: string[]
  fromTalents: string[]
}
